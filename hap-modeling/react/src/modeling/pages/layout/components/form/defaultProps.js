import { defaultLabelWidth } from 'choerodon-ui/pro/lib/form/utils';

export default () => ({
  props: {
    columns: 3,
    labelWidth: defaultLabelWidth,
    labelAlign: 'right',
    labelLayout: 'horizontal',
  },
});
